<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>ニュース - はとあげマート</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="はとあげマーケットの最新ニュース一覧です。">
  <link rel="stylesheet" href="/style.css?v=20260301">
  <script defer src="/script.js?v=20260301"></script>
  <link rel="icon" href="/assets/favicon.ico">
</head>
<body>
<%- include("partials/header") %>

<main class="news-main">
  <section class="news-hero">
    <p class="news-tag">HATOAGE BLOG</p>
    <h1>ニュース</h1>
    <p class="news-subtitle">はとあげマートの最新情報・お知らせをお届けします。</p>
  </section>

  <% if (error) { %>
    <p class="news-error"><%= error %></p>
  <% } %>

  <% if (!news || news.length === 0) { %>
    <p class="news-empty">現在、お知らせはありません。</p>
  <% } %>

  <section class="news-list">
    <% news.forEach(item => { %>
      <article class="news-post">
        <a class="news-link" href="/news/<%= item.uuid || item.id %>">
        <header class="news-post-header">
          <time datetime="<%= item.date %>" class="news-date"><%= item.date %></time>
          <h2><%= item.title %></h2>
        </header>
        <p class="news-body"><%= item.body || "本文は準備中です。" %></p>
        </a>
      </article>
    <% }) %>
  </section>
</main>

<%- include("partials/footer") %>
</body>
</html>
